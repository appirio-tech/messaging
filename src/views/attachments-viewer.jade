modal(show="vm.showImageSlideViewer" background-click-close=true)
  image-viewer-header(
    title ="{{vm.currentTitle}}"
    avatar="{{vm.currentAvatar}}"
    handle="{{vm.currentHandle}}"
    download-url="vm.currentImage.thumbnailUrl"
    download-allowed=true
  )

  image-slide-viewer(
    on-file-change="vm.onFileChange(file)"
    files="vm.currentImages"
    starting-file="vm.currentImage"
  )